let x = 1
let y = 2

// Write your code below this line.
if ( y > x ) {
    console.log(`Y is`, (y))
} else if (y === x) {
    console.log("The values are identical")
} else {
    console.log(`X is`, (x))
}
